
#include <gtest/gtest.h>
#include <gmock/gmock-matchers.h>

using namespace testing;

// TODO: Test Run function: window should show
TEST(FizzBuzzAppTests, SampleTest)
{
    EXPECT_EQ(1, 1);
    ASSERT_THAT(0, Eq(0));
}

// TODO: Test OnCalculateButtonClicked sets output as expected
// TODO: Create mock? So that connect is tested have mock that emits signal OnCalculateButtonClicked should be connected to

